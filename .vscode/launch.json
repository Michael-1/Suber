{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "App",
      "type": "firefox",
      "request": "attach",
      "url": "http://localhost:8081",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "API",
      "type": "node",
      "request": "attach",
      "processId": "${command:PickProcess}",
      "restart": true,
      "protocol": "inspector"
    }
  ]
}
